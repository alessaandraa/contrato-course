title: Contrato
description: Contrato de la materia de Contratos Mercantiles.
template:
  repo: contrato-template
  name: contrato
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
